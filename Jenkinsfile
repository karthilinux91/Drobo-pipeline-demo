pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        bat 'hostname'
      }
    }
    stage('test') {
      steps {
        bat 'hostname'
      }
    }
    stage('') {
      steps {
        bat 'hostname'
      }
    }
  }
}